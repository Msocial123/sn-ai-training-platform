apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: incident-watcher-state
  namespace: ai-agents
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-watcher
  namespace: ai-agents
spec:
  replicas: 1
  strategy:
    type: Recreate # ReadWriteOnce state PVC -- never run two pods at once
  selector:
    matchLabels: { app: incident-watcher }
  template:
    metadata:
      labels: { app: incident-watcher }
    spec:
      containers:
        - name: incident-watcher
          image: python:3.12-slim
          command: ["sh", "-c"]
          args:
            - "pip install --no-cache-dir --quiet -r /app/requirements.txt && exec uvicorn app:app --host 0.0.0.0 --port 8080 --app-dir /app"
          ports:
            - containerPort: 8080
          env:
            - name: SERVICENOW_INSTANCE_URL
              valueFrom: { secretKeyRef: { name: servicenow-oauth, key: instance-url, optional: true } }
            - name: SERVICENOW_CLIENT_ID
              valueFrom: { secretKeyRef: { name: servicenow-oauth, key: client-id, optional: true } }
            - name: SERVICENOW_CLIENT_SECRET
              valueFrom: { secretKeyRef: { name: servicenow-oauth, key: client-secret, optional: true } }
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { cpu: 300m, memory: 256Mi }
          volumeMounts:
            - { name: src, mountPath: /app/app.py, subPath: app.py }
            - { name: src, mountPath: /app/requirements.txt, subPath: requirements.txt }
            - { name: shared, mountPath: /app/shared }
            - { name: sample-data, mountPath: /app/data }
            - { name: state, mountPath: /app/state }
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 8
            periodSeconds: 5
      volumes:
        - name: src
          configMap: { name: incident-watcher-src }
        - name: shared
          configMap: { name: ai-agents-shared-lib }
        - name: sample-data
          configMap: { name: ai-agents-sample-data }
        - name: state
          persistentVolumeClaim: { claimName: incident-watcher-state }
---
apiVersion: v1
kind: Service
metadata:
  name: incident-watcher
  namespace: ai-agents
spec:
  selector: { app: incident-watcher }
  ports:
    - port: 8080
      targetPort: 8080
